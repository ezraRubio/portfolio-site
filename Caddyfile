# Root domain - serve static files
ezrarubio.com {
    root * /var/www/portfolio-site/refactor/dist
    file_server
}

# Subdomain - reverse proxy to Docker app
secrets.ezrarubio.com {
    reverse_proxy localhost:3000
}
